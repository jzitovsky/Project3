args = commandArgs(trailingOnly = TRUE)
col = table =  read.table(args[1], sep="\t", header=T, check.names = F)
col = table =  read.table(args[2], sep="\t", header=T, check.names = F)
col = table =  read.table(args[3], sep="\t", header=T, check.names = F)
master = list(collaborators = col, words = words, uniqueWords = unqWords)
saveRDS(master, "masterList.rds")
